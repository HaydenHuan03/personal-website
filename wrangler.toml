# Cloudflare Pages Configuration
name = "haydenhuan-com"
compatibility_date = "2024-12-14"

# Build configuration
[build]
command = "cd frontend && npm install && npm run build"
cwd = "."

# Static assets directory
[assets]
directory = "./frontend/dist"

# Note: Headers and redirects are configured via _headers and _redirects files
# in the frontend/public directory, which are copied to the dist folder during build
